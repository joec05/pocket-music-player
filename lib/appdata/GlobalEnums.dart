enum LoopStatus{
  repeatCurrent, repeatAll, shuffleAll 
}

enum DurationEndDisplay{
  totalDuration, timeRemaining
}

enum AudioPlayerState{
  playing, paused, stopped, completed
}

enum LoadType{
  initial, scan
}